import json; s = 'import json; s = %r; c = %r; js = %r; cpp = %r; java = %r; ruby = %r; rust = %r; python_code = s %% (s, c, js, cpp, java, ruby, rust); rust_code = rust %% json.dumps(python_code); ruby_code = ruby %% json.dumps(rust_code); java_code = java %% json.dumps(ruby_code); cpp_code = cpp %% json.dumps(java_code); js_code = js %% json.dumps(cpp_code); c_code = c %% json.dumps(js_code); print(c_code)'; c = '#include <stdio.h>\nint main() { char s[] = %s; printf("%%s", s); }'; js = 'const s = %s; console.log(s);'; cpp = '#include <iostream>\nint main() { std::string s = %s; std::cout << s; }'; java = 'public class Main { public static void main(String[] args) { String s = %s; System.out.print(s); } }'; ruby = 's = %s; print "%%s" %% [s]'; rust = 'fn main() { let s = %s; println!("{}", s); }'; python_code = s % (s, c, js, cpp, java, ruby, rust); rust_code = rust % json.dumps(python_code); ruby_code = ruby % json.dumps(rust_code); java_code = java % json.dumps(ruby_code); cpp_code = cpp % json.dumps(java_code); js_code = js % json.dumps(cpp_code); c_code = c % json.dumps(js_code); print(c_code)